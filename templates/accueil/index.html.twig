{% extends 'base.html.twig' %}

{% block title %}Blog SIO{% endblock %}

{% block body %}
    <h1 class="mt-3">Bienvenue sur le blog SIO</h1>
    <p>Ce site vous présente les dernières nouveautés du BTS SIO du lycée Louis Pergaud !</p>
    <p>Vous trouverez ci-dessous les 10 derniers articles parut !</p>
    <h2>Liste des articles</h2>
    <ul >
        {% for article in articles %}
            <div class="group-list border p-2 bg-light mt-4 mb-4 ">
                <li><a href="{{ path('app_accueil') }}"></a><h3>{{ article.titre }}</h3></li>
                <a class="btn btn-success" href="{{ path('app_article_slug',{"slug":article.slug}) }}">Détail</a>
                <p>Article du {{ article.createdat | date('d/m/Y') }}</p>
            </div>
        {% endfor %}
    </ul>

{% endblock %}
